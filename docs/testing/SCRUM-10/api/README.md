# API Test Cases

## Module

API Testing

## Phạm vi kiểm thử

- Authentication API (`/api/auth`)
- User Management API (`/api/users`)
- Category Management API (`/api/categories`)
- Product Management API (`/api/products`)
- Restaurant Table & QR API (`/api/tables`)
- Reservation API (`/api/reservations`)
- Cart API (`/api/cart`)
- Order Management API (`/api/orders`)
- Payment Gateways (`/api/momo`, `/api/payos`)
- Review API (`/api/reviews`)
- Point Loyalty API (`/api/points`)
- Statistics & Reports API (`/api/stats`)
- Image Upload API (`/api/upload`)

## Tổng số Test Case dự kiến

200 Test Cases

## Người thực hiện

Nguyễn Anh Huy

## Jira Ticket

SCRUM-10
